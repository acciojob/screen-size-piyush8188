name: Acciojob Tests

on:
 push:
    branches: [ "main","master" ]
 workflow_dispatch:
    inputs:
      environment:
        type: string
        required: true

jobs:
  checking-question-type:
    runs-on: ubuntu-latest
    steps:
    - name: Running main workflow
      uses: acciojob/acciojob-tests-action@main
      
